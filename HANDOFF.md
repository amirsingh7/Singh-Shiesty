# RESUME HERE
- **Working on:** PR Portfolio implementation. Phase 6 (AI layer, first slice: "Ask AI" plausibility review) is DONE, merged, live in production (PRs #10, #11). A follow-up data-loss bug fix (PR #12) is also merged and live.
- **Next step:** Waiting on the user to give specifics for a backfill of past lifts they have evidence for (which lift, date, weight × reps × sets, any missed sets, and where the evidence photos/videos are — local file path vs. their phone). Once given, write the Train history entries the same way the Aug 1 squat entry was backfilled (see Watch out), and if evidence files are on this computer, upload them to Supabase Storage + write matching `evidence` records so they badge as "Evidence Attached".
- **Waiting on you:** the backfill specifics above — no other blocker. Ask "what's the list?" if resuming cold.

-----

## Done so far
- **Phases 0–5**: real auth/multi-tenant isolation, truthful `RecordStatus` + share tokens, CSV/OpenPowerlifting import, evidence upload, timeline/witnessing/outlier-checks — all merged, all confirmed live. Full detail in memory `project_pr_portfolio.md` (kept current every phase).
- **Phase 6, first slice — AI plausibility review ("Ask AI")**: merged via PR #10 and #11, confirmed live. An "Ask AI" button on any **self-reported, tier-1 compound-lift** Timeline entry (not gated to outlier-flagged ones anymore — that was a follow-up per the user's request) calls Claude **Sonnet 5** (model chosen by the user after a cost/quality discussion) via a new owner-authenticated route, returns a cached plausibility verdict + one-sentence reasoning. Doesn't touch `recordStatus`/badge precedence — purely an annotation. Public `/p/[userId]` page shows the cached verdict read-only.
- **Data-loss bug found and fixed (PR #12)**: the user reported a logged Barbell Back Squat set missing from a real workout day. Root cause: the actual Supabase write for ANY tile save happens in the host page (`lib/tiles/useTileHost.ts`), not inside the sandboxed tile — closing the app while that write is in flight kills it silently, no error surfaced. Fixed by tracking in-flight saves and adding a native `beforeunload` "leave site?" prompt while any save is still writing. Also **directly backfilled the actual missing squat entry** into the user's real production data via a service-role script (245lb × 6 for 4 sets, 5th set 245lb × 4, 6th set missed/failed, dated 2026-08-01) — verified on read-back.
- User separately asked about individual-vs-org Anthropic Console account (chose Individual), funded $5 in Console credits, and picked Sonnet 5 over Opus 5/Haiku 4.5 for the AI review route after a cost discussion.

## Key files
- `app/api/ai/review-pr/route.ts` — the AI review route. Owner-authenticated only (session cookie via `supabaseServer()`). Uses `client.messages.parse()` + `zodOutputFormat` — **note it imports `z` from `'zod/v4'`, not the plain `'zod'`** other code in this repo uses (e.g. `app/api/mcp`) — `zodOutputFormat` needs the v4 compat submodule.
- `lib/tiles/aiReview.ts` — new store, `tile_id: 'ai_reviews'`, bare-key pattern like `competitions.ts`/`evidence.ts`/`witnesses.ts`, `reviewKey(liftId,date,w,r)`.
- `lib/tiles/profileDerive.ts` — `timelineEvents()`'s `outlier` flag is now scoped to `lift.tier === 1` only (accessory lifts never flagged — too noisy).
- `app/profile/ProfilePage.tsx` / `app/p/[userId]/page.tsx` — Timeline rendering for the outlier flag + AI verdict + "Ask AI" button (owner page only; public page is read-only, no button).
- `lib/tiles/useTileHost.ts` — the new `pendingSaves` ref + `beforeunload` guard (PR #12 fix). This is the ONE place any tile's actual Supabase write happens (sealed tiles have no network of their own).
- `.env.local` / `.env.example` — new `ANTHROPIC_API_KEY` (framed as a platform-operational key like `SUPABASE_SERVICE_ROLE_KEY`, not a per-user key like YouTube/Finnhub). **Already added to `.env.local`.** Was confirmed added to Vercel Production+Preview by the user during this session.

## Watch out
- **`ANTHROPIC_API_KEY` in Vercel**: user was walked through adding it via the dashboard (Settings → Environment Variables, both Production + Preview checked) and redeploying the correct PR-branch preview deployment (NOT Production, which was still on an old commit) — confirmed working. If the "Ask AI" button ever 503s in prod, check this first.
- **Two `tile_data` key conventions** still apply — `lib/sync.ts` (profile, competitions, evidence, witnesses, spotify_auth, ai_reviews) uses bare `tile_id` scoped by the `user_id` column; `lib/tiles/tileStore.ts` (every sealed tile: Train, Fuel, Vitals, etc.) prefixes the key itself as `${userId}:${tileId}`.
- **Playwright verification pattern**: `npm install --no-save playwright` + `npx playwright install chromium`, write a throwaway-account seed+verify script INSIDE the project root (not the scratchpad dir — Node won't resolve `node_modules` from outside the project), run it, then `rm` the temp script + `npm uninstall playwright` and confirm `package.json`/`package-lock.json` are untouched via `git status`.
- **Real production data edits** (the squat backfill) were done directly via a Node script using `SUPABASE_SERVICE_ROLE_KEY` + `OWNER_USER_ID` from `.env.local` — read-modify-write the exact `tile_data` row, verify on read-back. Only do this at the user's explicit, specific request (exact numbers/date given) — never on ambiguous asks.
- Repo has an auto-commit-and-push hook ("auto: update from Claude Code") — most commits happen automatically, usually within a few seconds to a minute. Check `git status`/`git log` before opening a PR to make sure it already fired.
- `gh pr merge <N> --merge` + a `Monitor` polling `curl https://singh-shiesty.vercel.app/` for 200 is the established flow for confirming a Vercel auto-redeploy landed after merging to `main`.
